<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <style>
        /* General Styles */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f5f8fa;
            color: #14171a;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            text-align: center;
        }

        /* Heading Styles */
        h1 {
            margin-bottom: 20px;
            color: #e74c3c;
            /* Red color for error indication */
        }

        /* Paragraph Styles */
        p {
            margin-bottom: 20px;
            font-size: 1em;
            color: #657786;
            /* Muted text color */
        }

        /* Link Styles */
        a {
            color: #1da1f2;
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s;
        }

        a:hover {
            color: #0d8bcf;
        }

        /* Responsive Styles */
        @media (max-width: 480px) {
            h1 {
                font-size: 1.8em;
            }

            p {
                font-size: 0.9em;
            }
        }
    </style>
</head>

<body>
    <h1>Error</h1>
    <p>
        <%= message %>
    </p>
    <p><a href="/">Go back to signup</a></p>
</body>

</html>